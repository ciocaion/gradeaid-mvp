import { useHistory } from 'react-router-dom';

function Features() {
    const history = useHistory();

    return (
        <div>
            <h1>Features Page</h1>
            // ... existing code ...
        </div>
    );
}

export default Features; 